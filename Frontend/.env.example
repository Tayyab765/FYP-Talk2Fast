# Leave unset in local dev to use Vite proxy (same port as the app → fewer CORS issues).
# Or set explicitly:
# VITE_API_BASE_URL=http://localhost:5000

# Groq API Key for AI explanations
VITE_GROQ_API_KEY=your_groq_api_key_here
